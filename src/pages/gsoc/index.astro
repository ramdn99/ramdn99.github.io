---
import Base from '../../layouts/Base.astro';
import EntryCard from '../../components/EntryCard.astro';
import TagFilterBar from '../../components/TagFilterBar.astro';
import { getCollection } from 'astro:content';
import { plainExcerpt, extractFirstImage } from '../../lib/excerpt';

const entries = (await getCollection('gsoc', ({ data }) => !data.draft))
  .sort((a, b) => a.data.title.localeCompare(b.data.title, undefined, { numeric: true, sensitivity: 'base' }));

const allTags = Array.from(new Set(entries.flatMap((e) => e.data.tags || []))).sort();
---
<Base title="./GSOC — ./ramdn.sh">
  <section class="dsec">
    <div class="dsec-head">
      <span class="dsec-prompt">$</span>
      <span class="dsec-title">tree ./GSOC --all</span>
      <span class="dsec-meta">{entries.length} modules</span>
    </div>

    <!-- GSOC Course Index Panel -->
    <div class="gsoc-index-panel">
      <div class="gsoc-index-header">
        <span class="gsoc-index-title">
          <span class="gsoc-prompt">$</span> cat SEC450-Index.md
        </span>
        <span class="gsoc-index-badge">MODULE INDEX</span>
      </div>
      <div class="gsoc-index-content">
        <div class="gsoc-module-title">SEC450.1 — Blue Team Tools and Operations</div>
        <div class="gsoc-index-grid">
          {entries.map((e) => (
            <a href={`/gsoc/${e.id}/`} class="gsoc-index-item">
              <span class="gsoc-item-name">{e.data.title}</span>
              <span class="gsoc-item-arrow">→</span>
            </a>
          ))}
        </div>
      </div>
    </div>

    <TagFilterBar tags={allTags} />

    <div class="card-grid">
      {entries.map((e) => (
        <EntryCard
          title={e.data.title}
          date={e.data.date}
          tags={e.data.tags}
          summary={e.data.summary}
          excerpt={plainExcerpt(e)}
          image={extractFirstImage(e)}
          readTime={e.data.readTime}
          href={`/gsoc/${e.id}/`}
        />
      ))}
    </div>
  </section>
</Base>

<style>
  .card-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(290px, 1fr));
    gap: 24px;
  }
  .gsoc-index-panel {
    background: var(--bg-panel);
    border: 1px solid var(--border-bright);
    border-radius: 12px;
    padding: 18px 20px;
    margin-bottom: 24px;
    font-family: var(--font-mono);
  }
  .gsoc-index-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 10px;
    margin-bottom: 14px;
    border-bottom: 1px dashed var(--border);
  }
  .gsoc-index-title {
    font-size: 0.88rem;
    font-weight: 700;
    color: var(--text);
  }
  .gsoc-prompt {
    color: var(--blue-bright);
    margin-right: 6px;
  }
  .gsoc-index-badge {
    font-size: 0.7rem;
    font-weight: 700;
    color: var(--blue-bright);
    background: rgba(47, 129, 247, 0.1);
    border: 1px solid rgba(47, 129, 247, 0.3);
    padding: 2px 8px;
    border-radius: 4px;
    letter-spacing: 0.05em;
  }
  .gsoc-module-title {
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--blue-bright);
    margin-bottom: 10px;
  }
  .gsoc-index-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
    gap: 8px 16px;
  }
  .gsoc-index-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 0.82rem;
    color: var(--text-dim);
    text-decoration: none;
    padding: 6px 10px;
    background: var(--bg-panel-2);
    border: 1px solid var(--border);
    border-radius: 6px;
    transition: all 0.15s ease;
  }
  .gsoc-index-item:hover {
    color: var(--blue-bright);
    border-color: var(--border-bright);
    transform: translateX(2px);
  }
  .gsoc-item-name {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  .gsoc-item-arrow {
    color: var(--blue-bright);
    margin-left: 8px;
  }
</style>
